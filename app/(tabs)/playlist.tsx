import { Text, View } from 'react-native';

export default function PlaylistScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-semibold text-black">Playlist</Text>
    </View>
  );
}
